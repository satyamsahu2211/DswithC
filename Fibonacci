#include <stdio.h>

int main() {
int n,a=0,b=1,i,c;
printf("enter the number of ");
scanf("%d",&n);
printf("%d\t%d",a,b);

for(i=3;i<=n;i++){
    c=a+b;
    a=b;
    b=c;
    printf("%d",c);
}
printf("enter the number",c);
    return 0;
}
